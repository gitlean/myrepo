----mysql-------------------------
create database ibatis;
use ibatis;

CREATE TABLE user_acount (
	userId int,
	username varchar(10),
	password varchar(30),
	groupname varchar(10)
);

select * from user_acount;

truncate table user_acount;
--------------------------------------------------


















